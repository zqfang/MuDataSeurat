interface:
  display_name: "MuDataSeurat"
  short_description: "Repo-specific patterns and workflows for MuDataSeurat"
  default_prompt: "Use the MuDataSeurat repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true